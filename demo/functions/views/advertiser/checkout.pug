extends _layout.pug

block content
    h2 Thanks for your purchase!
    div
        img.pixel(height='24' width='24' src=adtechRequestUrl) 
        p(style="font-size: 1.2em; color: red") Pixel that tracks purchases (completed checkouts)
    div(style="font-size: 1.2em") Order ID: #{purchaseId}
    //- button(onClick="generateNewPurchase()") 🔄 New purchase (buy something else)
    //- script.
    //-     async function generateNewPurchase() {
    //-         await fetch('/new-purchase', {
    //-             method: 'POST',
    //-             credentials: 'same-origin',
    //-             headers: {
    //-             'Content-Type': 'application/json'
    //-             },
    //-         })
    //-         location.reload()
        }
    //- script(src="./toast.js")